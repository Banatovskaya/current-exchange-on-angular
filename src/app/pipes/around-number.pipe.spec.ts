import { AroundNumberPipe } from './around-number.pipe';

describe('AroundNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AroundNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
